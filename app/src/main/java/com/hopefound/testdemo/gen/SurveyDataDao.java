package com.hopefound.testdemo.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hopefound.testdemo.entity.SurveyData;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SURVEY_DATA".
*/
public class SurveyDataDao extends AbstractDao<SurveyData, Long> {

    public static final String TABLENAME = "SURVEY_DATA";

    /**
     * Properties of entity SurveyData.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property QuestionName = new Property(1, String.class, "questionName", false, "QUESTION_NAME");
        public final static Property IsAnswed = new Property(2, boolean.class, "isAnswed", false, "IS_ANSWED");
    }

    private DaoSession daoSession;


    public SurveyDataDao(DaoConfig config) {
        super(config);
    }
    
    public SurveyDataDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SURVEY_DATA\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"QUESTION_NAME\" TEXT," + // 1: questionName
                "\"IS_ANSWED\" INTEGER NOT NULL );"); // 2: isAnswed
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SURVEY_DATA\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SurveyData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String questionName = entity.getQuestionName();
        if (questionName != null) {
            stmt.bindString(2, questionName);
        }
        stmt.bindLong(3, entity.getIsAnswed() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SurveyData entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String questionName = entity.getQuestionName();
        if (questionName != null) {
            stmt.bindString(2, questionName);
        }
        stmt.bindLong(3, entity.getIsAnswed() ? 1L: 0L);
    }

    @Override
    protected final void attachEntity(SurveyData entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SurveyData readEntity(Cursor cursor, int offset) {
        SurveyData entity = new SurveyData( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // questionName
            cursor.getShort(offset + 2) != 0 // isAnswed
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SurveyData entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setQuestionName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIsAnswed(cursor.getShort(offset + 2) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SurveyData entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SurveyData entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SurveyData entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
